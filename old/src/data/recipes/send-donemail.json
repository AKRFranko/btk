{
    "eval": {
        "args": {
            "php": "wp_mail( get_bloginfo('admin_email'), 'EDB Server Notification', 'Server '.site_url().' has been updated via: '.getenv('SSH_CONNECTION') );"
        },
        "opts": {
            "quiet": true
        }
    }
}